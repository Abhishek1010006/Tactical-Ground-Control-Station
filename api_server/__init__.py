# ==============================================================================
# api_server/__init__.py — Package Initializer for the SwarmGCS API Server
# ==============================================================================
# Marks the api_server directory as a Python package so its sub-modules
# (server.py, function.py, shared.py, map.py, terminal.py) can be imported
# using dotted paths like `from api_server.server import run`.
# ==============================================================================

"""Modular API package for the SwarmGCS Electron frontend."""
